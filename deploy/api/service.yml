apiVersion: v1
kind: Service
metadata:
  name: song-recommendation-api-service
spec:
  selector:
    app.kubernetes.io/name: lucassilva-song-recommendation-api
  ports:
    - protocol: TCP
      port: 30555
      targetPort: 30555